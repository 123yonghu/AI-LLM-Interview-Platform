# ========== AI 模拟面试助手 - 依赖清单 ==========
# 安装方式: pip install -r requirements.txt

# Web 框架
flask>=3.0

# LLM API 客户端（兼容 OpenAI SDK 协议） 
openai>=1.0

# 阿里云 DashScope（语音识别 ASR）
dashscope>=1.20

# PDF 文本提取
PyPDF2>=3.0

# 音频录制
pyaudio>=0.2

# TTS 语音合成（微软 Edge TTS，声音自然）
edge-tts>=6.0

# TTS 播放
pygame>=2.5

# 环境变量管理
python-dotenv>=1.0

# 安全加固（文件名处理）
werkzeug>=3.0
